{
  "name": "nnact-pro",
  "private": true,
  "version": "0.1.0",
  "description": "NNACT Pro — technical operations platform for HVAC, refrigeration, electrical, solar, and field maintenance teams.",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --parallel --filter ./apps/* dev",
    "dev:api": "pnpm --filter @nnact/api dev",
    "dev:web": "pnpm --filter @nnact/web dev",
    "build": "pnpm -r build",
    "db:generate": "pnpm --filter @nnact/db generate",
    "db:check": "pnpm --filter @nnact/db check",
    "db:parity": "pnpm --filter @nnact/db schema:parity",
    "db:migrate": "pnpm --filter @nnact/db migrate",
    "db:push": "pnpm --filter @nnact/db push",
    "db:seed": "pnpm --filter @nnact/db seed",
    "test": "pnpm -r test",
    "audit:dependencies": "node scripts/audit-dependencies.mjs",
    "test:release": "node --test scripts/*.test.mjs",
    "lock:prepare": "pnpm install --lockfile-only --no-frozen-lockfile --reporter=silent && node scripts/verify-lockfile.mjs",
    "install:verified": "pnpm lock:prepare && pnpm install --frozen-lockfile --reporter=silent",
    "release:safety": "node scripts/release-safety-check.mjs && node scripts/verify-lockfile.mjs && pnpm test:release",
    "infra:up": "docker compose -f infra/compose.yml up -d",
    "infra:down": "docker compose -f infra/compose.yml down"
  },
  "pnpm": {
    "overrides": {
      "brace-expansion": "5.0.9",
      "fast-jwt": "6.2.4",
      "fast-uri": "3.1.5",
      "find-my-way": "9.7.0",
      "js-yaml@^3": "3.15.1",
      "js-yaml@^4": "4.3.1",
      "nanoid": "3.3.18",
      "postcss": "8.5.23",
      "sharp": "0.35.0",
      "tar": "7.5.22",
      "undici": "6.28.0",
      "@xmldom/xmldom": "0.8.13",
      "uuid": "11.1.1",
      "esbuild": "0.25.12"
    },
    "patchedDependencies": {
      "brace-expansion@5.0.9": "patches/brace-expansion@5.0.9.patch"
    }
  },
  "engines": {
    "node": ">=22"
  }
}
